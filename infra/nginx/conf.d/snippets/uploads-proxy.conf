# Static uploads served by Nest API (uploads volume)
location /uploads/ {
  proxy_pass http://cardon_api/uploads/;
  include /etc/nginx/conf.d/snippets/proxy-headers.conf;
  expires 7d;
  add_header Cache-Control "public, max-age=604800";
}
