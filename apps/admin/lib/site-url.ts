/** @deprecated Import from `@/lib/public-url` instead. */
export {
  getFrontendUrl as getPublicBaseUrl,
  getFrontendUrl as getSiteUrl,
  buildPublicPageUrl,
  resolvePublicUrl,
} from '@/lib/public-url';
